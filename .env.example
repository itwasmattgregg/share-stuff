DATABASE_URL="file:./dev.db"
SESSION_SECRET="super-duper-s3cret"

# Optional SMTP settings for password reset emails.
# Without these, reset links are logged to the server console in development.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="ShareStuff <noreply@example.com>"

# Optional Cloudflare R2 settings for item photo uploads.
# Without these, items work normally but photo uploads are disabled.
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
