# NPM Configuration for ShareStuff
# This file configures npm behavior for the project

# Use npm registry (default)
registry=https://registry.npmjs.org/

# Save exact versions in package.json
save-exact=true

# Use npm ci for faster, reliable builds
engine-strict=true

# Cache configuration (using prefer-offline instead of deprecated cache-min)

# Security settings
audit-level=moderate
fund=false

# Logging
loglevel=warn

# Performance optimizations
prefer-offline=true
progress=false

# Lock file configuration
package-lock=true

# Node.js version requirement (if using .nvmrc)
# This ensures compatibility with the project's Node.js version
